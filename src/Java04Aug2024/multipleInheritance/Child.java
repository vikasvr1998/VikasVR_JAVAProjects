package Java04Aug2024.multipleInheritance;

public class Child // extends Father , Mother
{
  void c()
  { // A child cannot access the methods from both the same parent and hence java does not allow this
   //   home();
   //   money();
  }
}